import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
    appContainer: {
        display: 'flex',
        flexDirection: 'row',
        paddingTop: 20,
        paddingBottom: 20,
        width: '70%',
        marginLeft: 'auto',
        marginRight: 'auto'
    },
});

export default useStyles;